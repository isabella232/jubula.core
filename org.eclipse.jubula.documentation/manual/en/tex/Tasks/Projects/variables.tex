% BREDEX LaTeX Template
%  \documentclass is either ``bxreport'' or ``bxarticle''
%                 option is bxpaper
%% \documentclass{bxarticle}
%% % ----------------------------------------------------------------------
%% \begin{document}
%% \title{}
%% \author{}
%% % \author*{Hauptautor}{Liste der Nebenautoren}
%% \maketitle
%% % ----------------------------------------------------------------------
%% \bxversion{0.1}
%% %\bxdocinfo{STATUS}{freigegeben durch}{freigegeben am}{Verteilerliste}
%% \bxdocinfo{DRAFT}{}{}{}
%% % ----------------------------------------------------------------------

%% \end{document}
%% \index{Event confirm timeout}
%% \index{Click delay}
%% \index{JRE parameters}
%% \index{Parameter!JRE}

%% \begin{enumerate}
%% \item In the \gdaut{} configuration dialog (expert level) \bxpref{configuringaut} you can set JRE variables for
%% an \bxname{event confirm timeout} and for a \bxname{click delay}.

%% \textbf{Event confirm timeout:}
%% The timeout variable tells the server how long to wait to receive confirmation that an action was successful. The default timeout is 2000ms (2 seconds). If no confirmation has been received at the end of this time, the \gdstep{} is marked as failed. 

%% \item To change the timeout variable, enter \bxshell{-DGDCONFIRM=<ms>} in the field for the JRE parameters in the \gdaut{} configuration dialog.
%% \item The value after the equals sign is the timeout in
%% milliseconds. For example, in \bxcaption{-DGDCONFIRM=3000} the timeout
%% is 3 seconds. 

%% \textbf{Click delay:}
%% The click delay variable tells \gd{} how long to wait before performing each mouseclick. The default value is 1000ms (1 second).
%% \item To change the click delay variable, enter \bxshell{-DGDDELAY=500} in the field for the JRE parameters in the \gdaut{} configuration dialog.
%% \item The value after the equals sign is the delay in
%% milliseconds. For example, in \bxcaption{-DGDDELAY=500}, the click delay is half a second. 

\begin{enumerate}
\item In the \gdaut{} configuration dialog (expert level) \bxpref{configuringaut} you can set JRE variables.
\item Any user-defined variables entered here have precedence over variables you have specified for your system. 
\item For more information on user-defined variables, see the section later \bxpref{TasksSystemVariables}. 
\end{enumerate}
