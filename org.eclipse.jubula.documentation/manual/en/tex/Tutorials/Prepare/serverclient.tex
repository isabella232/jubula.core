\begin{enumerate}
\item Start the license server (if it is not already running as a service) via:\\
\bxmenu{\jb{}}{Start license server}{}
\item Start the \gdagent on the default port number via:\\ \bxmenu{\jb{}}{Start \gdagent}{}.  
\item If for any reason you cannot start the \gdagent on this port number, see later \bxpref{otherportnumber} for details on starting the \gdagent on another port number. 
\item Start the client via the start menu:\\ \bxmenu{\jb{}}{\jb{}}{}. 
\item When prompted, choose a workspace and click \bxcaption{OK}. 
\end{enumerate}
