\begin{enumerate}
\item Start the \gdagent on the default port number via:\\ \bxmenu{\app{}}{Start \gdagent}{}.  
\item If for any reason you cannot start the \gdagent on this port number, see later \bxpref{TasksAgentExternal} for details on starting the \gdagent on another port number. 
\item Start the \ite{} via the start menu:\\ \bxmenu{\app{}}{\app{}}{}. 
\item When prompted, choose a workspace and click \bxcaption{OK}. 
\end{enumerate}
