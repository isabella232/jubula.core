\begin{enumerate}
\item Start the license server (if it is not already running as a service) via:\\
\bxmenu{\gd{}}{Start license server}{}
\item Start the \gdserver on the default port number via:\\ \bxmenu{\gd{}}{Start \gdserver}{}.  
\item If for any reason you cannot start the \gdserver on this port number, see later \bxpref{otherportnumber} for details on starting the \gdserver on another port number. 
\item Start the client via the start menu:\\ \bxmenu{\gd{}}{\gd{}}{}. 
\item When prompted, choose a workspace and click \bxcaption{OK}. 
\end{enumerate}
