% CAP description for Web Browser --> Refresh components

\begin{itemize}
\item Use this action to refresh the component hierarchy in your web application. 
\item This can be useful for web applications with dynamic components. The refresh allows \gd{} to locate and recognize components that have been added dynamically. 
\end{itemize}
