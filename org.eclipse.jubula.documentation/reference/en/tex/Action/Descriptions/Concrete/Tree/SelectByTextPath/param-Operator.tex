% CAP description for Tree --> Select by Textpath --> Operator

\input{Action/Descriptions/Common/param-Operator}

\bxtipp{When you use a regular expression for a treepath, bear in mind that 
each subpath is considered separately. The first path in the
tree which corresponds to each subpath will be chosen. It is therefore 
advisable to make your regular expressions as unambiguous as possible.}