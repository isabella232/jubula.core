% CAP description for Tree --> Collapse by Textpath --> Use Regular Expression
   %%  Set to true to evaluate ''Textpath'' as a regular expression. In
%%     this case, each part of the textpath
%%     (Separated by a slash, '{\tt{}/}' ) is
%%     handled as an individual regular expression. In each case, only the
%%     first match is used. See \bxref{regex} for more information about
%%     using regular expressions in \GD{}.
