For SWT and RCP \gdauts{}, you need to define a keyboard layouts for the \gdaut{} (i.e. which keyboard layout is set for the system on which the \gdaut{} runs).

\gd{} already has the keyboard layouts for German (Germany) and English (US) saved. If you want to use another keyboard layout, follow the instructions in the next section to create one.  
